"""Claude Agent SDK project for the indie game development iteration loop.

This package defines the 7 design agents, the 16 implementation roles, and the
2 QA roles described in `docs/team_roles/_main.md` / `design_roles.md`, plus the
8-phase iteration-loop logic from `docs/workflow/`. It exposes the loop over HTTP
so the n8n workflows in `docs/workflow/n8n/` can orchestrate it.
"""

__version__ = "0.1.0"
