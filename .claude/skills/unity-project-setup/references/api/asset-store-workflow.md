<!-- source: /home/reza/projects/game/docs/UnityDocumentation/Documentation/en/Manual/asset-store-workflow.html
     Unity 6 (6000.x) — converted by unity_html_to_md.py.
     Doc-sourced; not compile-tested in this environment. -->

# Asset package Asset Store publishing workflow

You can create an **asset package** (`.unitypackage`) from assets that you own and make it available to download on Unity’s Asset Store. To do this, follow the Asset Store publishing workflow which requires personal information, information about your package, and approval from Unity’s Asset Store team.

**Note:** For information about the legal requirements an Asset Store package must meet, read the [Asset Store Provider Agreement](https://unity3d.com/legal/as_provider) and the [Submission Guidelines](https://unity3d.com/asset-store/sell-assets/submission-guidelines).

## Prerequisites

To publish assets to the Asset Store, you must first create the following:

1. [Create a Unity ID](https://id.unity.com/account/new).
2. Set up an Asset Store publisher profile.

## Publish an asset package to the Asset Store

To create and publish assets as an asset package (`.unitypackage`), do the following:

1. In the publisher portal, create a package draft.
2. In the Unity Editor:
  1. Validate assets.
  2. Upload assets.
3. In the publisher portal:
  1. Submit a package for approval.
  2. Check the status of an Asset Store package submission.

When your package is available on the Asset Store, you can manage it in the following ways:

- Update an Asset Store package
- Manage Asset Store package sales and revenue
- Deprecate an Asset Store package
- Add a user to a publisher profile

## Additional resources

- [Managing your Organization](https://docs.unity.com/cloud/en-us/)
- Asset Store packages
- Introduction to importing assets
