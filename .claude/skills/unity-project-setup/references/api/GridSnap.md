<!-- source: /home/reza/projects/game/docs/UnityDocumentation/Documentation/en/Manual/GridSnap.html
     Unity 6 (6000.x) — converted by unity_html_to_md.py.
     Doc-sourced; not compile-tested in this environment. -->

# Snap a GameObject to the grid

When you enable automatic grid snapping, the **Move** transform tool snaps the selected **GameObject** to the grid along the active **gizmo** axis.

If you want the GameObject to move in smaller increments, you can modify the values it uses to transform incrementally.

If grid snapping is enabled but you want to move a GameObject without snapping, hold down **Control** (macOS: **Command**) while you use the **Move** tool.

To enable automatic snapping to the grid:

1. Enable the **Move** tool in the Tools overlay.
2. In the **Tool Settings** overlay, set the handle orientation to **Global** or **Grid**.
3. In the **Grid and Snap** overlay, enable **Toggle absolute Grid snapping**.

## Additional resources

- Scene view grid snapping
- Grid and Snap overlay reference
- Overlays
- The Scene view
- Position GameObjects
- Move, rotate, and scale in increments
