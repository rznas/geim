<!-- source: /home/reza/projects/game/docs/UnityDocumentation/Documentation/en/Manual/SnapIncrements.html
     Unity 6 (6000.x) — converted by unity_html_to_md.py.
     Doc-sourced; not compile-tested in this environment. -->

# Move, rotate, and scale in increments

Use incremental snapping to move, rotate, or scale **GameObjects** by precise, discrete values.

To move, rotate, or scale by increment snap values when incremental snapping is not enabled, hold down **Control** (macOS: **Command**) while you use one of the transform gizmos in the **Scene** view.

## Move a GameObject in increments

To enable incremental snapping for the Move tool, select **Toggle incremental Grid snapping** in the **Grid and Snap** overlay. To control the snap distance, set a value for **Incremental snapping size**.

## Rotate a GameObject in increments

To enable incremental snapping for the Rotate tool, select **Toggle angle snapping** in the **Grid and Snap** overlay. To control the rotation snapping, set a value for **Incremental angle snap size**.

## Scale a GameObject in increments

To enable incremental snapping for the Scale tool, select **Toggle scale snapping** in the **Grid and Snap** overlay. To control the scale snapping, set a value for **Scale snap multiplier**.

## Additional resources

- Scene view grid snapping
- Grid and Snap overlay reference
- Overlays
- The Scene view
- Position GameObjects
