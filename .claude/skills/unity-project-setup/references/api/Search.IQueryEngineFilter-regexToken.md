<!-- source: /home/reza/projects/game/docs/UnityDocumentation/Documentation/en/ScriptReference/Search.IQueryEngineFilter-regexToken.html
     Unity 6 (6000.x) — converted by unity_html_to_md.py.
     Doc-sourced; not compile-tested in this environment. -->

### Description

The regular expression that matches the filter. Matches what precedes the operator in a filter (for example. "id" in "id>=2").

This regular expression token is set when you create a filter with a regular expression. If you create a filter without a regular expression, token will be set and the regexToken will be null.
