# StarForge — Godot 4.6 C# project setup result

Project created at `/tmp/eval-starforge` using the `godot-project-setup` skill
(Godot 4.6.3-stable mono + .NET SDK 10.0.109; SDK pinned to `Godot.NET.Sdk/4.6.3`
from the editor's bundled nupkgs). A copy of the full project is in
`eval-starforge/` alongside this file.

## Files created

Scaffolded by `scripts/new_project.sh`:

- `project.godot` — declares the `"C#"` feature and a `[dotnet]` section
  (`project/assembly_name="StarForge"`); `run/main_scene` added by hand (below)
- `StarForge.csproj` — `Sdk="Godot.NET.Sdk/4.6.3"`, `net8.0`,
  `EnableDynamicLoading`, `Nullable=enable`, `RootNamespace=StarForge`
- `nuget.config` — bundled editor nupkgs feed first (offline-safe), nuget.org second
- `.gitignore` — ignores `.godot/`, `bin/`, `obj/`, `*.user`
- `scripts/`, `scenes/`, `assets/art/`, `assets/audio/` — source layout

Added by hand for the minimal runnable scene:

- `scripts/Main.cs` — `public partial class Main : Node`, prints in `_Ready()`
- `scenes/Main.tscn` — one-node scene referencing the script
- edited `project.godot` to add `run/main_scene="res://scenes/Main.tscn"`
  inside the existing `[application]` section (not appended at the tail)
- `scripts/Main.cs.uid` — generated by Godot on first import

## Build

Exact command (run via the skill's two-gate checker):

```
bash scripts/build_and_check.sh /tmp/eval-starforge
# gate 1: dotnet build /tmp/eval-starforge/StarForge.csproj
# gate 2: godot --headless --path /tmp/eval-starforge --import
```

Result: **SUCCEEDED.** `dotnet build` reported `Build succeeded. 0 Warning(s)
0 Error(s)` and produced `.godot/mono/temp/bin/Debug/StarForge.dll`. The
headless `--import` gate finished `OK: build + import clean.`

## Run (headless)

Exact command:

```
bash scripts/godot.sh --headless --path /tmp/eval-starforge --quit-after 2
```

Literal stdout printed:

```
Godot Engine v4.6.3.stable.mono.official.7d41c59c4 - https://godotengine.org

StarForge online: C# is running
```

The `_Ready()` print line seen was:

```
StarForge online: C# is running
```
