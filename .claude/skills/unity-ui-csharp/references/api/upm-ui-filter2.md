<!-- source: /home/reza/projects/game/docs/UnityDocumentation/Documentation/en/Manual/upm-ui-filter2.html
     Unity 6 (6000.x) — converted by unity_html_to_md.py.
     Doc-sourced; not compile-tested in this environment. -->

# Filter the package list

To narrow down which packages appear in the list panel, select the **Filters** menu from the **Package Manager** window.

**Note:** Selecting **All Packages** in the navigation panel disables the filter controls. You can check for packages that need updates by using the dedicated view, **Updates**.

Using the **My Assets** list as an example, you can use the following types of filter options to narrow down the list of **Asset Store** packages displayed:

- The **Status** group lets you choose one **Status** to list the packages that have that status. For example, if you choose **Unlabeled**, then only those Asset Store packages that you never assigned a label to appear in the list.
- Choose one or more **Categories** to list the Asset Store packages that match your selection.
- Choose one or more custom **Labels** that you created in the Asset Store for your packages. If you select more than one label, only those packages that match any one label remain in the list. For information about creating and managing labels, refer to Using labels to organize My Assets.

If you need to start over, click **Clear Filters** beside the **Filters** menu to reset all filter options to their default values.
